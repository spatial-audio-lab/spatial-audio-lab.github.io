name: Deploy Audio Shapes to Subfolder

on:
  push:
    branches: ['main']

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
<<<<<<< HEAD
          cache: "npm"
          # Wskazujemy ścieżkę do lockfile w podfolderze
          cache-dependency-path: "./audio-shapes/package-lock.json"
=======
          cache: 'npm'
          # Wskazujemy ścieżkę do lockfile w podfolderze
          cache-dependency-path: './audio-shapes/package-lock.json'
>>>>>>> a59833ad0f934819bd9d2ab27616894a990b5476

      - name: Install dependencies
        run: npm ci
        working-directory: ./audio-shapes

      - name: Build
        run: npm run build
        working-directory: ./audio-shapes

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Prepare Deployment Folder
        # To jest najważniejszy krok! Tworzymy strukturę docelową.
        run: |
          mkdir -p public_deployment/audio-shapes
<<<<<<< HEAD

          # 1. Kopiujemy Twoją stronę główną (root)
          # Jeśli masz plik index.html w głównym folderze repozytorium:
          rsync -av --exclude='audio-shapes' --exclude='.git' --exclude='.github' --exclude='public_deployment' ./ public_deployment/

=======
          
          # 1. Kopiujemy Twoją stronę główną (root)
          # Jeśli masz plik index.html w głównym folderze repozytorium:
          rsync -av --exclude='audio-shapes' --exclude='.git' --exclude='.github' --exclude='public_deployment' ./ public_deployment/
          
>>>>>>> a59833ad0f934819bd9d2ab27616894a990b5476
          # 2. Kopiujemy ZBUDOWANĄ aplikację (z folderu dist) do podfolderu
          cp -r audio-shapes/dist/* public_deployment/audio-shapes/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Wysyłamy folder, który przygotowaliśmy powyżej
<<<<<<< HEAD
          path: "./public_deployment"
=======
          path: './public_deployment'
>>>>>>> a59833ad0f934819bd9d2ab27616894a990b5476

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
